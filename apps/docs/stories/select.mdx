import { Meta, Controls, Primary } from '@storybook/addon-docs/blocks';
import * as SelectStories from './select.stories';
import * as SelectTriggerStories from './select-trigger.stories';
import * as SelectContentStories from './select-content.stories';
import * as SelectItemStories from './select-item.stories';
import * as SelectGroupStories from './select-group.stories';
import * as SelectLoadingStories from './select-loading.stories';

<Meta of={SelectStories} />

# Select

Lightweight dropdown select built on Radix UI Select. Use for single or multi-value selection without search. For searchable dropdowns, use Combobox.

For the quick-start preset, see [SelectSimple](?path=/docs/composed-components-selectsimple--docs).

## Basic select (composition)

For full control, compose the subcomponents manually:

```tsx
import {
	Select,
	SelectTrigger,
	SelectContent,
	SelectItem,
	SelectGroup,
	SelectLabel,
	SelectSeparator,
} from '@signozhq/ui';
import { useState } from 'react';

const frameworks = [
	{ value: 'react', label: 'React' },
	{ value: 'vue', label: 'Vue' },
];

export default function MyComponent() {
	const [value, setValue] = useState('');

	return (
		<Select value={value} onChange={(v) => setValue(v as string)}>
			<SelectTrigger placeholder="Select a framework..." />
			<SelectContent>
				{frameworks.map((f) => (
					<SelectItem key={f.value} value={f.value}>
						{f.label}
					</SelectItem>
				))}
			</SelectContent>
		</Select>
	);
}
```

<Primary />

## Select vs Combobox

| Feature | Select | Combobox |
|---------|--------|----------|
| Search/filter | No | Yes |
| Multi-select | Yes (with pills) | Manual |
| Best for | Simple dropdowns | Large lists, async search |

## Subcomponents

### SelectTrigger

<Controls of={SelectTriggerStories.Default} />

### SelectContent

<Controls of={SelectContentStories.Default} />

### SelectItem

<Controls of={SelectItemStories.Default} />

### SelectGroup

<Controls of={SelectGroupStories.Default} />

### SelectLoading

<Controls of={SelectLoadingStories.Default} />
